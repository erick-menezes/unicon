// Firebase data types

export interface CourseDataType {
    id: string;
    name: string;
}

// Form data types

export interface RegisterUserFormDataType {
    email: string;
    name: string;
    course: string;
    password: string;
}